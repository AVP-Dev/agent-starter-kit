# Project: [НАЗВАНИЕ ПРОЕКТА]

> Этот файл — ЕДИНЫЙ ИНДЕКС И СТАНДАРТ РАЗРАБОТКИ (Single Source of Truth).
> Открытый стандарт `AGENTS.md` (Agentic AI Foundation / Linux Foundation), нативно поддерживаемый всеми современными автономными ИИ-агентами: **Gemini Antigravity**, **Claude Code**, **Cursor**, **Windsurf**, **Roo Code**, **Cline**, **Codex**, **GitHub Copilot**, **Aider**, **OpenCode** и др.
> Симлинк для Claude Code: `CLAUDE.md -> AGENTS.md`.
> Методологическая база: [AI loves straight roads](https://avpdev.com/ru/blog/ai-loves-straight-roads/) и [Architect of the Future](https://avpdev.com/ru/blog/architect-of-the-future/).

---

## 1. Назначение и контекст проекта
[Краткое описание проекта: 2-3 предложения о назначении, целевой аудитории и ключевых функциях].

---

## 2. Актуальный технологический стек (Стандарт 2026)

> ⚡ **Zero-Friction Auto-Onboarding:** Данный раздел актуализируется ИИ-агентом **автоматически** при запуске протокола онбординга ([docs/onboarding-protocol.md](docs/onboarding-protocol.md)) на основе реальных манифестов кодовой базы (`package.json`, `pnpm-lock.yaml`, `pyproject.toml`, `go.mod`, `Cargo.toml`, `Dockerfile`).
> Список ниже — эталонный стек 2026 года для новых проектов. При онбординге существующего репозитория агент адаптирует этот раздел под фактический стек проекта без ручной работы человека.

- **Среда выполнения и языки:** Node.js 26 LTS / Bun 1.3+ / Python 3.13–3.14 / Go 1.27+ / TypeScript 5.8+
- **Фронтенд:** Next.js (App Router) / React / Astro (SSG/контент) / Tailwind CSS
- **Бэкенд и фоновые задачи:** Next.js Server Actions / Route Handlers, FastAPI (Python), Go микросервисы, REST API, WebSockets, BullMQ (очереди задач)
- **Базы данных и кэш:** PostgreSQL, Drizzle ORM, Redis (кэширование, pub/sub, очереди)
- **ИИ, RAG и Агенты:** LLM API (Anthropic Claude, OpenAI, Gemini), RAG-пайплайны (pgvector, HNSW-индексы), ONNX Runtime (локальный/self-hosted инференс), Agent Orchestration & Tool Calling
- **Инфраструктура и деплой:** Docker, Coolify / Dokploy (PaaS self-hosted), GitHub Actions (CI/CD), Linux (Bash, Nginx), Git / GitHub

### Правило актуализации библиотек и версий (Context7 & Discrepancy Policy):
1. **Приоритет Context7 MCP / Поиска:** При написании кода интеграций с библиотеками, SDK или фреймворками агент **обязан** проверить актуальный синтаксис через Context7 MCP (`resolve-library-id` -> `query-docs`) или веб-поиск, чтобы исключить галлюцинации устаревших API.
2. **Политика расхождений версий (Discrepancy Policy):**
   - Если между установленной в проекте версией и актуальной документацией обнаружены **критические расхождения / breaking changes** — агент **обязан спросить пользователя**, требуется ли миграция, прежде чем вносить изменения.
   - Если расхождения незначительны и обратно совместимы — использовать текущую стабильную версию без лишнего шума.

### Онбординг без потери данных
- **Brownfield:** фактический стек, команды проверки, структура и документация извлекаются из существующего проекта; факты не выспрашиваются у человека.
- **Greenfield:** агент задаёт только вопросы, ответы на которые нельзя получить из репозитория, и предлагает значения по умолчанию.
- **Ручной режим:** пользователь может заполнить или исправить любое поле вручную; ручные значения имеют приоритет.
- **Режим обновления:** существующие `AGENTS.md`, `docs/*.md`, `CLAUDE.md` и проектные файлы не перезаписываются без явного подтверждения; недостающие стандартные файлы добавляются отдельно.

---

## 3. Структура репозитория

```
[корень]/
├── src/ (или apps/ + packages/)    # Исходный код приложения
├── docs/                          # Инженерная документация
│   ├── architecture.md            # Архитектурный каркас (Skeleton + Modules)
│   ├── theme-tokens.md            # Контракт дизайн-токенов и тем оформления (Zero Hardcoded Colors)
│   ├── decisions.md               # Реестр архитектурных решений (ADR)
│   ├── state.md                   # Сессионный журнал состояния (в работе / техдолг)
│   ├── glossary.md                # Единый глоссарий доменных терминов
│   ├── agent-routing.md            # Адаптивная маршрутизация и контроль контекста
│   ├── project-structure.md        # Правила структуры, именования и размера файлов
│   ├── verification.md             # Impact, safety и test coverage policy
│   ├── verification-profile.example.tsv # Шаблон project-local команд
│   └── design-manifest-template.md# Шаблон предпроектного манифеста (Zero Vibe Coding)
├── tests/                         # Автотесты (юнит-, интеграционные, e2e)
├── scripts/                       # Скрипты автоматизации и миграций
│   ├── verify-project.sh          # Dispatcher project-local verification profile
│   ├── security-scan.sh           # Dependency-free secret baseline
│   └── verification-profile.tsv   # Generated project commands; не хранить секреты
├── CLAUDE.md                      # Симлинк на AGENTS.md (для Claude Code)
└── AGENTS.md                      # Единый SSOT правил разработки
```

---

## 4. Жёсткие правила (Zero Tolerance — нарушать нельзя)

### 4.1. Архитектура: «Skeleton + Pluggable Modules»
1. **Skeleton (Каркас):** Проектируется Человеком-Архитектором.
   - Единая точка валидации входных данных (Zod / Pydantic).
   - Единая схема данных (SSOT) и контекст сессии/пользователя.
   - Шина событий/очередей и централизованная обработка ошибок.
2. **Pluggable Modules (Сменные модули):** Наполняются ИИ-агентом.
   - Каждый модуль замкнут в границах своего типизированного контракта.
   - **Запрет скрытого сцепления:** Модуль НЕ имеет прямого доступа к чужим приватным таблицам БД или чужому состоянию.
   - **Принцип заменяемости (Disposable Modules):** При изменении требований модуль перегенерируется с нуля по обновленному контракту, а не затягивается в недели спагетти-рефакторинга.

### 4.2. Матрица делегирования ответственности (Delegation Matrix)
Любая задача разделяется строго по уровню ответственности:

| Уровень задачи | Ответственный | Скоуп |
|---|---|---|
| **Зона 1: Полное делегирование (100% AI)** | ИИ-агент автономно | Чистые функции без сайд-эффектов, DTO-конвертеры, утилиты, верстка UI-компонентов по дизайн-системе, парсеры данных, юнит-тесты краевых условий |
| **Зона 2: Контрактное делегирование (50 / 50)** | Человек согласует контракт → ИИ реализует | CRUD-эндпоинты, обработчики очередей задач, интеграции внешних API, Server Actions, генерация отчетов. Агент не пишет код до фиксации сценариев сбоев (Failure Modes) |
| **Зона 3: Нулевое делегирование (0% AI / Только человек)** | Архитектор (ручной контроль) | Модель владения данными и схема БД, финансовые транзакции, модель прав (RBAC/ABAC), архитектура безопасности. Запрещено модифицировать агенту без Confirmation Gate |

### 4.3. Предпроектный DESIGN-манифест (Zero Vibe Coding)
Перед написанием кода для любой нетривиальной задачи (новый API-эндпоинт, фоновый воркер, интеграция, модель данных или модуль) агент **ОБЯЗАН** зафиксировать краткий манифест (см. [docs/design-manifest-template.md](docs/design-manifest-template.md)):
1. **Инварианты системы (Invariants):** Что *НЕ МОЖЕТ* произойти ни при каких обстоятельствах.
2. **Конечный автомат (State Machine):** Четкие состояния сущности (`Draft → Pending → Completed / Failed`) и допустимые переходы.
3. **Границы сбоев (Failure Modes & Idempotency):**
   - Поведение при сетевых таймаутах и отказах внешних сервисов (fail-closed vs fallback).
   - Защита от дублирующих запросов и повторных вебхуков (ключи идемпотентности).
4. **Критерии доказательства качества (Acceptance Invariants):** Список конкретных негативных сценариев и стресс-проверок.

### 4.4. Качество кода и обработка ошибок
1. **Абсолютный запрет `any`:** Все типы строго описываются интерфейсами или схемами валидации.
2. **Абсолютный запрет глушения ошибок:**
   - ❌ Запрещено: пустые блоки `catch {}`, `catch (e: any)`, `.catch(console.error)`.
   - ✅ Обязательно: типизация `catch (error: unknown)`, явное извлечение сообщения и проброс/логирование контекста.
3. **Fail-Closed валидация на границах:** Все данные из внешнего мира валидируются схемами ДО передачи в бизнес-логику.
4. **Non-Destructive Data Policy:** Категорически запрещено решать проблемы рендеринга или трансформации удалением данных (`DROP`, `TRUNCATE`, сброс таблиц). Ошибки чинятся в логике резолверов и обратимыми миграциями.
5. **Запрет блокирующих вызовов в UI:** Никаких синхронных `confirm()` или `alert()`, ломающих мобильные браузеры (iOS Safari).

### 4.5. Стандарт интерфейса, дизайн-токенов и визуальных тем (Theme-Driven Visual Architecture)
1. **Инвариант «Zero Hardcoded Colors in Components»:**
   - Категорически запрещено зашивать в верстку компонентов сырые hex-коды (`#1e293b`), arbitrary values или жестко зашитые классы базовых палитр (напр. `bg-zinc-900`, `text-blue-500`).
   - Вся стилизация строится **исключительно на семантических дизайн-токенах / CSS-переменных**:
     `--background`, `--foreground`, `--primary`, `--primary-foreground`, `--muted`, `--muted-foreground`, `--accent`, `--accent-foreground`, `--border`, `--input`, `--ring`, `--radius`.
   - Любая тема (светлая, тёмная, корпоративная, нео-брутализм, скандинавская, киберпанк и др.) объявляется как изолированный набор значений токенов (`:root`, `.dark`, `[data-theme="..."]`).
   - Добавление, замена или переключение темы оформления в проекте обязаны производиться **без модификации кода компонентов** (Zero Component Edits).
2. **Иерархия действий и типографика (Anti-AI-Slop):**
   - На одном экране/вьюпорте допускается не более одного действия первого порядка (Primary CTA). Второстепенные действия оформляются строго как Secondary, Outline или Ghost.
   - Запрещены однообразные плоские карточки с размытыми тенями без четких границ и контраста.
   - Типографическая шкала строго фиксирована через токены проекта (например, `text-xs` .. `text-3xl`, tracking, line-height).
3. **Состояния элементов и интерактивность:**
   - Каждый интерактивный элемент обязан иметь явно описанные состояния: `:hover`, `:active`, `:focus-visible` (доступный фокусный контур для клавиатурной навигации), `:disabled` и состояние загрузки (`loading state / skeleton`).
4. **Семантика и доступность (Fail-Closed a11y):**
   - Запрет кликабельных `<div>` или `<span>` со слушателями клика — строго нативные `<button>`, `<a>`, `<input>` с доступными `aria-label` при отсутствии явного текста.
   - Минимальный коэффициент контрастности текста к фону: не менее 4.5:1 (WCAG AA).
   - Мобильная адаптивность (Mobile-First): верстка гарантированно не ломается и не имеет горизонтального скролла в диапазоне от 360px до 4K.

### 4.6. Адаптивная маршрутизация и дисциплина контекста
Подробный протокол находится в [docs/agent-routing.md](docs/agent-routing.md). В начале нетривиальной задачи основной агент работает как `orchestrator` и выбирает topology: `orchestrated`, `single`, `fixed-pipeline`, `side-research`, `parallel` или `hierarchical`.
1. Оркестратор определяет контракт, делегирует содержательную работу изолированным worker-агентам, проверяет evidence, запускает gates, интегрирует и принимает результат; он не должен незаметно выполнять основную реализацию сам.
2. `single` допускается только для тривиальной, одношаговой, детерминированной задачи или явного fallback при отсутствии delegation capability.
3. Детерминированное решение проверяется до привлечения LLM; гипотезы отделяются от проверенных фактов.
4. Перед заявлением «невозможно» агент проверяет релевантные файлы, конфигурацию, документацию или безопасный минимальный probe.
5. Handoff передаёт короткий контекстный пакет, а не полный чат; side research возвращает evidence и не меняет канонические документы.
6. После утверждённого плана reasoning можно снизить для механической реализации; при конфликтах, новых ошибках и неоднозначности его повышают.
7. После каждого этапа сохраняется короткий checkpoint с Run ID, evidence и следующим шагом; длинные логи и сырые транскрипты не накапливаются в основном контексте.
8. При неожиданностях и новых требованиях агент запрашивает решение человека, если изменение выходит за согласованный контракт.
9. Существующие тесты нельзя ослаблять ради зелёного результата; изменение ожидаемого поведения требует нового контракта.
10. Fallback не меняет права доступа, риск, доступ к данным или смысл задачи; изменения бюджета и времени фиксируются в run, а превышение лимита блокирует операцию.

### 4.7. Структура проекта и гигиена файлов
Подробные правила находятся в [docs/project-structure.md](docs/project-structure.md). Агент не создаёт гигантские файлы, не смешивает ответственности, не добавляет `utils`/`helpers`/`misc` без конкретной роли и не централизует код без минимум двух независимых потребителей. Перед структурным рефакторингом фиксируются ownership, scope и причина крупного файла в DESIGN-манифесте.

### 4.8. Verification, impact и безопасность
Подробный протокол находится в [docs/verification.md](docs/verification.md). Перед изменением агент проверяет impact surface и baseline, затем выбирает positive, negative, security, regression и integration проверки. Неясный риск требует targeted probe, side-research или повышения reasoning; повторять тот же цикл без новых данных запрещено.

---

## 5. Автоматическая верификация (Automated Verification Gate)

Чтобы вайб-кодинг был устойчивым и автоматизированным, агент **НЕ СЧИТАЕТ задачу завершенной**, пока не пройден гейт автоматической проверки:

1. **Обязательный запуск команд верификации:**
   - **TypeScript / JavaScript:**
     ```bash
     pnpm typecheck   # либо: npx tsc --noEmit
     pnpm lint        # проверка линтером (ESLint / Biome)
     pnpm test        # прогон автотестов
     ```
   - **Python:**
     ```bash
     ruff check .     # линтинг и форматирование
     mypy .           # строгая проверка типов
     pytest           # прогон автотестов
     ```
   - **Go:**
     ```bash
     go vet ./...
     golangci-lint run
     go test -v ./...
     ```
   - **Project-local profile:** если onboarding создал `scripts/verification-profile.tsv`, запускай `./scripts/verify-project.sh`; отсутствующий или невалидный профиль блокирует gate.
2. **Green-Bar Requirement:** Любая ошибка линтера, типов или тестов должна быть устранена до передачи ответа пользователю.
3. **Обязательное покрытие тестами каждой итерации:**
   - Любая новая функция/эндпоинт сопровождается юнит- или интеграционным тестом.
   - Тестируется не только Happy Path, но и минимум 1-2 краевых/негативных сценария (сбой сети, невалидные входные данные, дублирование ключа).

---

## 6. Безопасный Git / GitHub протокол и атомарные коммиты

### 6.1. Safe Remote Sync (Проверка актуальности перед началом)
Перед началом работы или крупной итерации агент обязан проверить синхронизацию с удаленным репозиторием:
```bash
git fetch origin && git status -uno
```
- ⚠️ **Правило обнаружения отставания:** Если локальная ветка отстает от удаленной (`Your branch is behind 'origin/...' by N commits`):
  - **Агент ОБЯЗАН ОСТАНОВИТЬСЯ** и уведомить пользователя.
  - **Категорически запрещен слепой `git pull`:** Автоматический pull может вызвать скрытые мерж-конфликты. Пользователь должен подтвердить синхронизацию.

### 6.2. Checkpoint и атомарные коммиты в конце каждой итерации
Checkpoint — это состояние процесса, evidence и следующий шаг; он не равен Git commit.
После успешного прохождения шагов (Код написан → Тесты написаны → Automated Verification Gate зеленый) агент фиксирует изменения коммитом по стандарту Conventional Commits:
```bash
git add <конкретные_файлы>
git commit -m "feat(module): добавить обработку RAG-пайплайна с pgvector"
# или fix(...), test(...), refactor(...)
```

Правила большой работы:
1. После каждого checkpoint обновить task ledger и указать `last good commit`; checkpoint сам по себе не изменяет Git-историю.
2. Один commit — одна атомарная задача или вертикальный срез; не объединять несвязанные worker-результаты.
3. Перед commit проверить `git status`, `git diff`, `git diff --cached` и отсутствие чужих неизвестных изменений.
4. Использовать только явный список файлов; запрещён `git add .` и автоматическое staging всего рабочего дерева.
5. Не коммитить красный или неполный результат, не ослаблять тесты ради зелёного статуса и не коммитить непроверенные secrets/конфигурацию.
6. Push, pull request и публикация выполняются отдельным явным действием после принятия commit, а не скрытым шагом onboarding.

---

## 7. Confirmation Gate Protocol (Запрос подтверждения)

Если задача требует:
- изменения схемы БД или миграций;
- изменения системы авторизации или прав доступа (RBAC);
- изменения межсервисных контрактов или протоколов безопасности,
агент **ОБЯЗАН остановиться** и предоставить человеку:
1. **Root Cause Analysis:** Точная причина проблемы.
2. **Impact Matrix:** На какие компоненты и сервисы повлияет изменение.
3. **План изменений:** И дождаться явного подтверждения от пользователя ДО редактирования файлов.

---

## 8. Заметки о сессии (Session Ledger)

Перед завершением рабочей сессии или крупного этапа агент обязан:
1. Обновить [docs/state.md](docs/state.md) — что сделано, что сломано, что осталось.
2. Если принято ключевое архитектурное решение — добавить запись в формате ADR в [docs/decisions.md](docs/decisions.md).
3. Сообщить статус покрытия тестами и пройденных проверок.

**Исключение для стартер-кита:** до установки кита в целевой проект `docs/state.md` остаётся универсальным шаблоном. После онбординга целевого проекта правило обновления state применяется к нему в обычном порядке.
