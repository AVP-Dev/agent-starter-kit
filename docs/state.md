# Текущее состояние проекта

> Обновляется в конце рабочей сессии или этапа разработки.
> Этот файл экономит контекст и токены, отражая актуальный срез: «Что сделано, что сломано, что делать дальше».

---

###### Последнее обновление
Дата: 2026-09-11 (Актуализация стандартов 2026 Edition: Verification Gates, Safe Git Sync, Modern Stack)

**Ключевые результаты сессии:**
1. **Унификация сред и удаление legacy-файлов:**
   - Удалены монолитные устаревшие правила `.cursorrules` и `.windsurfrules`.
   - Добавлен стандарт для OpenAI Codex / GitHub Copilot: `.github/copilot-instructions.md -> AGENTS.md`.
   - Сохранен стандарт Claude Code: `CLAUDE.md -> AGENTS.md`.
   - Централизован SSOT в `AGENTS.md` для Gemini Antigravity.
2. **Внедрение Automated Verification Gate:**
   - Внедрено жесткое требование: задача считается сданной только при прохождении команд `typecheck`, `lint` и `test` (Green Bar).
   - Обязательное покрытие каждой итерации юнит/интеграционными тестами (включая негативные кейсы).
3. **Безопасная синхронизация Git / GitHub и атомарные коммиты:**
   - Проверка отставания локальной ветки через `git fetch origin && git status -uno`.
   - Запрет автоматического слепого `git pull` при отставании от remote — обязательное уведомление пользователя.
   - Фиксация каждой успешной итерации атомарным коммитом (Conventional Commits).
4. **Актуализация стека (Стандарт 2026):**
   - Node 26 LTS, Bun 1.3+, Python 3.13–3.14, Go 1.27+, TypeScript 5.8+.
   - Next.js (App Router), Astro, FastAPI, BullMQ, Redis, PostgreSQL, Drizzle ORM.
   - RAG-пайплайны: pgvector, HNSW, ONNX Runtime, LLM APIs (Anthropic, OpenAI, Gemini).
   - Деплой и инфраструктура: Docker, Coolify, Dokploy, CI/CD.
   - Политика проверки через Context7 MCP / поиск и согласование breaking changes.

**Что сейчас в работе:**
- Стартовый кит готов к тиражированию в новые проекты.

**Что сломано / Известный техдолг:**
- Нет.

**Следующие шаги:**
1. Использование скрипта `scripts/init-project.sh` для инициализации новых репозиториев.
2. При создании целевого проекта заполнить шапку `AGENTS.md` конкретными деталями фич.
